export enum EOLS {
	lf = "\n",
	crlf = "\r\n",
	cr = "\r",
}

export enum SettingIds {
	style = "style",
	translation = "translation",
	test = "test",
	barrel = "barrel",
}

export enum ExportType {
	all = "all",
	named = "named",
	default = "default",
	defaultNamed = "defaultNamed",
}
